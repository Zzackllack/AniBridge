# AniBridge Example Environment File
# Copy this file to `.env` and adjust values as needed.

# --- Logging ---
LOG_LEVEL=INFO
# Set the logging level. Options: DEBUG, INFO, WARNING, ERROR. Default is INFO.

# --- Download Directories ---
# If running in Docker, default is '/data/downloads/anime', otherwise './data/downloads/anime'.
DOWNLOAD_DIR=./data/downloads/anime
# Directory where downloaded anime files will be saved.

DATA_DIR=./data
# Directory for app data (database, caches, snapshots).

# --- Title Source ---
ANIWORLD_ALPHABET_HTML=./data/aniworld-alphabeth.html
# Path to a local HTML snapshot of the AniWorld alphabet page. Used for resolving anime titles. If not set, will use live URL.

ANIWORLD_ALPHABET_URL=https://aniworld.to/animes-alphabet
# URL to fetch the AniWorld alphabet page live. Used if local HTML is not available or outdated.

ANIWORLD_TITLES_REFRESH_HOURS=24
# How often (in hours, float allowed) to refresh the live AniWorld titles index. Set to 0 to never refresh (only load once per process).

# --- Release Naming ---
SOURCE_TAG=WEB
# Source tag used in release names (e.g., WEB, WEB-DL).

RELEASE_GROUP=aniworld
# Release group name appended to the end of release names.

# --- Provider Fallback ---
PROVIDER_ORDER=VOE,Filemoon,Streamtape,Vidmoly,SpeedFiles,Doodstream,LoadX,Luluvdo,Vidoza
# Comma-separated list of streaming providers, in order of priority for fallback.

# --- Download Concurrency ---
MAX_CONCURRENCY=3
# Maximum number of concurrent downloads (thread pool size). Minimum is 1.

# --- Torznab / Indexer Configuration ---
INDEXER_NAME=AniBridge Torznab
# Name for the Torznab indexer.

INDEXER_API_KEY=
# Optional API key for Torznab indexer. If set, must match ?apikey=... in requests.

TORZNAB_CAT_ANIME=5070
# Category ID for anime (Torznab/Newznab). Default is 5070.

# --- Availability Cache ---
AVAILABILITY_TTL_HOURS=24
# TTL (in hours, float allowed) for semi-cache of episode quality & language.

# --- Fake Seeder/Leecher for Torznab-Items ---
TORZNAB_FAKE_SEEDERS=999
TORZNAB_FAKE_LEECHERS=787

# --- Torznab Test Configuration ---
TORZNAB_RETURN_TEST_RESULT=true
# If true, Torznab test endpoint returns a test result item. Default: true.

TORZNAB_TEST_TITLE=AniBridge Connectivity Test
# Title for the Torznab test result item. Default: AniBridge Connectivity Test.

TORZNAB_TEST_SLUG=connectivity-test
# Slug for the Torznab test result item. Default: connectivity-test.

TORZNAB_TEST_SEASON=1
# Season number for the Torznab test result item. Default: 1.

TORZNAB_TEST_EPISODE=1
# Episode number for the Torznab test result item. Default: 1.

TORZNAB_TEST_LANGUAGE=German Dub
# Language for the Torznab test result item. Default: German Dub.
