# Cloudflare Pages/Workers builds use `npm ci`, and npm treats prerelease versions
# (like vitepress@2.0.0-alpha.*) as not satisfying broad peer ranges by default.
# Allow npm to resolve peer deps without failing the install.
legacy-peer-deps=true
